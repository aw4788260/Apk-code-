// settings.gradle
pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// (تم حذف بلوك dependencyResolutionManagement)

rootProject.name = "SecureApp"
include ':app'
