// settings.gradle

// [ ✅✅✅ هذا هو الإصلاح الرئيسي ]
// (قمنا بإزالة 'maven { url 'https://jitpack.io' }' من هنا)
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) 
    repositories {
        google()
        mavenCentral()
        // (Jitpack تم حذفه)
    }
}

rootProject.name = "SecureApp"
include ':app'
