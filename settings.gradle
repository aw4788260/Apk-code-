// settings.gradle

// [ ✅✅✅ هذا هو الإصلاح الرئيسي ]
// (قمنا بإزالة 'maven { url 'https.jitpack.io' }' نهائياً)
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) 
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "SecureApp"
include ':app'
